# Flyway.conf for the test environment

# Note: This should probably all be auto-generated from env vars by deploy.sh.
#flyway.url=jdbc:mysql://<HOST>:<PORT>/<DBNAME>
#flyway.user=<USER>
#flyway.password=<PASSWORD>
